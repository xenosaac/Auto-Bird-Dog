OPENAI_API_KEY=sk-svcacct-REPLACE_ME
TAVILY_API_KEY=tvly-dev-REPLACE_ME

# Optional: model overrides. Leave commented to use defaults.
# CODEX_MODEL=gpt-5-codex
# CODEX_API=responses
# FALLBACK_MODEL=gpt-4o
# FALLBACK_API=chat

# Optional: first name used in cold-email signature.
# Defaults to "Sender" if unset. NO other identity fields are read.
SENDER_FIRST=

# Optional: deployed Vultr URL — flips the sponsor pill to "live" if set.
VULTR_URL=
